{
	"name": "server",
	"private": true,
	"scripts": {
		"dev": "uv run flask --app app run --debug --port 3000",
		"start": "uv run flask --app app run --host 0.0.0.0 --port 3000",
		"lint": "uv run ruff check . && uv run ty check .",
		"format": "uv run ruff format ."
	}
}
