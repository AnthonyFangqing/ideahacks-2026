[project]
name = "server"
version = "0.1.0"
description = "Flask backend for Ideahacks."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.32",
]
